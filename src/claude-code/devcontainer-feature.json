{
  "id": "claude-code",
  "version": "1.0.0",
  "name": "Claude Code",
  "description": "Installs Claude Code CLI for AI-powered development assistance",
  "options": {
    "remoteUser": {
      "type": "string",
      "default": "vscode",
      "description": "The remote user that will be used in the container. Must match the container's remote user."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/node"
  ],
  "mounts": [
    {
      "source": "${localEnv:HOME}/.claude",
      "target": "/home/${options.remoteUser}/.claude",
      "type": "bind"
    }
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "anthropic.claude-code"
      ]
    }
  }
}
